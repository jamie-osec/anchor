# Static map from Solana release → bundled `platform-tools` version.
#
# Source of truth: `DEFAULT_PLATFORM_TOOLS_VERSION` (or its predecessor, the
# `platform_tools_version = String::from("vX.Y")` literal) in cargo-build-sbf:
#
#   1.17.x – 1.18.x : solana-labs/solana, sdk/cargo-build-sbf/src/main.rs
#   2.0.x – 2.3.x : anza-xyz/agave, sdk/cargo-build-sbf/src/main.rs
#                   (then platform-tools-sdk/cargo-build-sbf/src/main.rs from 2.2)
#   3.0.x+ : anza-xyz/agave, platform-tools-sdk/cargo-build-sbf/src/toolchain.rs
#
# Resolution: given a requested Solana version `S`, pick the entry with the
# largest `solana` key that is `<= S`. Entries MUST be ordered by `solana` so
# the resolver can stop at the first key that exceeds the request.
#
# `rustc` is the Rust compiler bundled in the corresponding platform-tools
# release. Source of truth: `src/version` in anza-xyz/rust on the
# `solana-tools-<platform_tools>` branch.
#
# EXPERIMENT: the point releases below are successful platform-tools PR builds,
# not published releases yet. `ci_artifacts` maps them to the Actions run that
# produced their archives. AVM downloads those temporary artifacts through
# nightly.link until the corresponding GitHub releases exist.
#
# `cargo_lock_v4` records whether the bundled Cargo rejects lockfile v4
# (`unsupported`), accepts it only with `-Znext-lockfile-bump` (`opt-in`), or
# accepts it without an unstable opt-in (`native`).
#
# `fallback` is used when the project does not pin a Solana version at all.
# Keep it equal to the platform-tools version of the newest entry below.

fallback = "v1.56"

[ci_artifacts]
"v1.42.1" = 33803112214
"v1.46.1" = 33426681142
"v1.51.1" = 33425918843

[[entries]]
solana = "1.17.25"
platform_tools = "v1.37"
rustc = "1.68.0"
cargo_lock_v4 = "unsupported"

[[entries]]
solana = "1.18.0"
platform_tools = "v1.39"
rustc = "1.72.0"
cargo_lock_v4 = "opt-in"

[[entries]]
solana = "1.18.8"
platform_tools = "v1.42.1"
rustc = "1.75.0"
cargo_lock_v4 = "opt-in"

[[entries]]
solana = "2.0.5"
platform_tools = "v1.42.1"
rustc = "1.75.0"
cargo_lock_v4 = "opt-in"

[[entries]]
solana = "2.1.0"
platform_tools = "v1.46.1"
rustc = "1.79.0"
cargo_lock_v4 = "native"

[[entries]]
solana = "2.2.1"
platform_tools = "v1.46.1"
rustc = "1.79.0"
cargo_lock_v4 = "native"

[[entries]]
solana = "2.2.3"
platform_tools = "v1.46.1"
rustc = "1.79.0"
cargo_lock_v4 = "native"

[[entries]]
solana = "2.2.8"
platform_tools = "v1.46.1"
rustc = "1.79.0"
cargo_lock_v4 = "native"

[[entries]]
solana = "2.2.11"
platform_tools = "v1.51.1"
rustc = "1.84.1"
cargo_lock_v4 = "native"

[[entries]]
solana = "2.2.15"
platform_tools = "v1.51.1"
rustc = "1.84.1"
cargo_lock_v4 = "native"

[[entries]]
solana = "3.0.0"
platform_tools = "v1.51.1"
rustc = "1.84.1"
cargo_lock_v4 = "native"

[[entries]]
solana = "3.1.1"
platform_tools = "v1.56"
rustc = "1.89.0"
cargo_lock_v4 = "native"

[[entries]]
solana = "4.0.0"
platform_tools = "v1.56"
rustc = "1.89.0"
cargo_lock_v4 = "native"
